# Database_connectivity
### This is a C# Console Application that performs basic CRUD operations (Create, Read, Update, Delete) on a SQL Server database named student_db.

- The application allows users to:
- Insert student records
- Update existing records
- Delete records (single or multiple)
- View all records
- Execute SQL queries from a .txt file

